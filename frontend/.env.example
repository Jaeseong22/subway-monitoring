# 프론트엔드 환경 변수 예시
# 로컬 개발(npm run dev) 시 이 파일을 .env로 복사해 값을 채웁니다.
#   cp .env.example .env
# Docker Compose 빌드에서는 docker-compose.yml의 build args로 주입됩니다.

# 백엔드 API 주소 (브라우저에서 호출)
VITE_API_URL=http://localhost:8080

# Google OAuth 웹 클라이언트 ID (로그인 버튼 렌더링에 사용)
VITE_GOOGLE_CLIENT_ID=
